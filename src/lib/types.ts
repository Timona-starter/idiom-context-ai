// This file can be used to define shared types.
// For this project, scenario types are co-located with the data in `scenarioData.ts`

export type User = {
    id: string;
    name: string;
    avatarUrl: string;
    contributionScore: number;
};
  
